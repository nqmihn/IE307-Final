import { Text } from "react-native";
import { View } from "react-native";

const Order = () => {
  return (
    <View>
      <Text className="text-center">Order</Text>
    </View>
  );
};

export default Order;
